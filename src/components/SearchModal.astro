---
---

<div x-data="{ open: false }" @keydown.window.meta.k.prevent="open = true" @keydown.window.ctrl.k.prevent="open = true">
  <button @click="open = true" class="flex items-center gap-2 rounded-full border border-paper-300 bg-white/90 px-4 py-2 text-sm text-ink-500 shadow-panel hover:text-ink-900">
    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"/></svg>
    <span class="hidden sm:inline">Search...</span>
    <kbd class="hidden rounded-full border border-paper-300 bg-paper-50 px-2 py-0.5 text-xs text-ink-400 sm:inline">⌘K</kbd>
  </button>

  <div x-show="open" x-transition.opacity @click.away="open = false" @keydown.escape.window="open = false"
       class="fixed inset-0 z-[100] flex items-start justify-center pt-[15vh] bg-black/50 backdrop-blur-sm">
    <div @click.stop class="mx-4 w-full max-w-xl overflow-hidden rounded-[1.75rem] border border-paper-300 bg-paper-50 shadow-panel-lg">
      <div id="search-container" class="p-4"></div>
    </div>
  </div>
</div>

<script>
  document.addEventListener('DOMContentLoaded', () => {
    const container = document.getElementById('search-container');
    if (container && window.PagefindUI) {
      new window.PagefindUI({ element: container, showSubResults: true });
    }
  });
</script>
